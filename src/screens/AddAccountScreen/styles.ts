import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  addAccountContainer: {
    paddingTop: 35,
    paddingLeft: 20,
    paddingRight: 20,
  },

  formContainer: {
    alignItems: "center",
    gap: 20,
  },

  input: {
    width: '100%',
  }
});
